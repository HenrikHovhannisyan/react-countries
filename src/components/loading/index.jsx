import './loading.scss';
import React, { memo } from 'react';
import { Box } from '@mui/material';

const Loading = () => {
    return <Box className='loading'></Box>
}

export default memo(Loading);
